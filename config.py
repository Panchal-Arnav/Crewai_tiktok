# config.py

# LLM model to use with litellm
LLM_MODEL = "gpt-4o-mini"

# TikToker settings (if any API keys or configs needed)
TIKTOKER_CONFIG = {
    # Add config if needed
}

# Logging config
LOG_LEVEL = "INFO"